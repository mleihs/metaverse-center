# Fortsetzungs-Prompt (nach Context-Clear einfügen)

> Stand: 31.08.2026, Sitzung `velgarien-rebuild-88`. Alles unten ist gemessen,
> nichts geschätzt. **Alles committet, NICHTS gepusht.**

---

Setze den Umsetzungsplan aus der Systemprüfung vom 30.08.2026 weiter um —
händisch, Schritt für Schritt, keine Agenten.

ARBEITSWEISE: Arbeite so lange wie möglich DURCHGEHEND. Mach zuerst ALLES, was
keine Entscheidung von mir braucht. Frag mich erst, wenn wirklich nichts
Entscheidungsfreies mehr übrig ist — und dann gebündelt. Kein „soll ich
weitermachen?", kein Zwischenbericht nach jedem Commit.

LIES ZUERST:
1. Memory `pakete-g-h-i-d-j-2026-08-31.md` (mein Stand, Rezepte, offene
   Entscheidungen) und `a-door-that-only-opens-for-those-inside.md`.
2. `docs/plans/system-review-remediation-2026-08-30.md` — §0 Arbeitsregeln sind
   verbindlich; Paket J (J1–J8) und die Befunde N1–N5 am Ende sind neu.
3. `docs/analysis/warum-keine-events-2026-08-31.md` — der wichtigste Befund des
   Tages (N5), vollständig vermessen.
4. `handoff/email-redesign.md` — P0/P1 erledigt, P2 läuft bei der
   Parallelsitzung, **P3.27 und P3.28 sind meine und offen**.

ZUERST PRÜFEN:
- `git log --oneline origin/main..HEAD` — ist noch nichts gepusht?
- Ticken die Welten? `select slug, last_heartbeat_tick, last_heartbeat_at from
  simulations where slug in ('velgarien','the-m-bius-academy')`
  (Stand 05:19 UTC: Velgarien 50, Akademie 41 — beide laufen)
- `select count(*) from events where created_at > now() - interval '1 day'`
  → steht auf 0, und das ist **erklärt** (N5), kein neuer Befund.

DANN OHNE RÜCKFRAGE ABARBEITEN:
- **Paket D, Rest:** D10 (acht Kleinigkeiten — Punkt 4 ist angefangen und
  gemessen, siehe unten), D13 (Rest), D12 (soweit ohne Balance-Zahlen).
  D6 und der Kern von D12 hängen an der N5-Entscheidung.
- **Paket H:** H2 (125 deutsche Zeilen in `de.xlf` — meins, die
  Parallelsitzung fasst die Datei nicht an), H5 (Suchindex indiziert nur
  ~10 %). H1/H6/H7 hat die Parallelsitzung angeboten bekommen — vorher per
  `ListAgents`/`SendMessage` abstimmen, wer sie hat.
- **Mail-Handoff P3.27** (Admin-Vorschau-Route `/admin/emails/preview/{template}`
  mit den Fixtures aus `scripts/send_test_emails.py`) und **P3.28**
  (Eigenschaftstests: Kontrastverhältnis jeder Akzentfarbe, Preheader
  vorhanden, Text-Teil vorhanden, `List-Unsubscribe` gesetzt, keine
  `@keyframes`).

REGELN: nach jeder Änderung `ruff check backend` + `cd frontend && npm run
lint:full` + betroffene pytest. Migrationen im Wegwerf-Postgres proben
(Rezept unten), zweimal echt anwenden. **Prod-SCHREIBvorgänge NUR mit meinem
ausdrücklichen Wort; Prod LESEN ist frei.** Werte messen, nicht Zähler. Vor
jedem Deploy die Signaturkopplung prüfen (`git diff origin/main..HEAD` nach
`fn_*`-Aufrufen). `velg-frontend-design`-Skill vor jedem Komponentencode.
CLAUDE.md gilt vollständig.

GETEILTER ARBEITSBAUM mit `velgarien-rebuild-45`: nie `git stash`, nur
explizite Pfade stagen, `git status` vor jedem Commit. Sie hält Paket E, N1
und den kompletten Mail-P2 (`email_templates.py`,
`cycle_notification_service.py`, `epoch_cycle_scheduler.py`,
`lifecycle_mail_scheduler.py`, `backend/app.py`, `supabase/templates/`).
**Migrationsnummern: 295 und 299 sind meine (gebaut, nicht auf Prod);
296/297/298 sind ihre (296/297 auf Prod). Nächste freie: 300** — vor der
Vergabe abstimmen.

---

## Wo D10 steht (angefangen, gemessen, noch nicht gebaut)

Acht Punkte. Punkt 4 ist vollständig vermessen und der lohnendste:

**D10-4 — `agent_opinion_service.py:405-450`, zwei Defekte in einem Block:**
- Der Durchbruch-Zweig prüft `if not existing` (Entdopplung), der Bruch-Zweig
  prüft gar nichts — dasselbe Paar meldet in JEDEM Tick erneut einen
  `relationship_breakdown`.
- Und die Entdopplung ist ohnehin wirkungslos: die Konstanten heißen
  `RELATIONSHIP_CREATE_THRESHOLD` („auto-create positive relationship") und
  `RELATIONSHIP_HOSTILE_THRESHOLD`, aber **nichts fügt je in
  `agent_relationships` ein** (gemessen: kein einziger `insert` im ganzen
  Backend). Die 48 Bestandsbeziehungen stammen aus der Schmiede, nicht aus
  dem Spiel.
- Zusammenhang: das ist derselbe Grund, aus dem der Regler
  `autonomy_relationship_auto_create` (Paket A4) tot ist — die Automatik wurde
  nie gebaut.
- Schema für den Insert: `agent_relationships(simulation_id, source_agent_id,
  target_agent_id, relationship_type, is_bidirectional=true, intensity=5,
  description, metadata)`. Vorhandene Typen auf Prod: `antagonist`,
  `rival`, `mentor`, `informant`, `trading_partner`, u. a. (12 Stück).
- ⚠ Wirkt heute nicht sichtbar, weil keine Meinung die Schwelle ±60 erreicht
  (Höchstwert 45, siehe N5). Trotzdem bauen — sonst spammt es, sobald N5
  entschieden ist.

**Die übrigen sieben:**
1. `zone_action_service.py:44-113` — Prüfen-dann-Einfügen ohne Transaktion,
   zwei gleichzeitige Anfragen kommen beide durch. Atomare RPC (ADR-007),
   braucht eine Migration.
2. `WeatherSettingsPanel.ts:200-244` schreibt nach `simulations.weather_lat/lon`
   — Endpunkt fehlt.
3. `event_service.py:520-522` — falscher Settings-Schlüssel
   (`heartbeat_interval_seconds`).
5. `agent_activity_service.py:684-691` — `stacking_group` fehlt am Moodlet.
6. `page_size` → `limit`/`page` an vier Stellen.
7. `stream_single_response` ohne `budget=`.
8. `fn_degrade_building` schreibt einen Taxonomiewert.

## Wo D13 steht

Erledigt: die zwei toten Komponenten unter `components/resonance/` gelöscht
(je 1 100 Zeilen, null Referenzen; Resonanzen werden im
`SimulationsDashboard` und im Admin bereits angezeigt).

Gemessen, noch offen:
- `enter_strain` / `recover_from_strain` in `bond_service.py` — vollständig
  gebaut, **null Aufrufer**. Der dokumentierte Lebenszyklus lautet
  `forming → active → strained → farewell`; auf Prod existiert **genau EINE
  Bindung, Status `forming`**. Verdrahten braucht eine Zahl („ab wann ist eine
  Bindung vernachlässigt?") → **Entscheidung**.
- `resonance_operative_*_cap`: existiert weder in `platform_settings` noch im
  Code. Der Deckel 0,04 steht **fest in SQL** (`fn_resonance_operative_modifier`).
  Konfigurierbar machen wäre eine Migration für eine Konstante, die nie
  jemand ändern musste (1 Resonanz auf Prod) — geringer Wert, gemeldet.
- `bleed_auto_approve`: nur im Frontend-Panel, kein Leser → gehört zu **D9**
  (Entscheidung).
- **Formel-Doppelung** `AgentDetailsPanel._computeInfluence`: der Client rechnet
  Einfluss selbst und weicht vom Server ab — der Server nimmt die STÄRKSTEN
  fünf Beziehungen (`ORDER BY intensity DESC LIMIT 5`), der Client die ersten
  fünf in beliebiger Reihenfolge; der Server prüft eine aktive Botschaft plus
  `ambassador_blocked_until`, der Client ein Flag `is_ambassador`.
  ⚠ Der Serverwert ist NICHT abrufbar: `fn_compute_agent_influence` speist nur
  `mv_building_readiness`, es gibt kein agentenbezogenes Feld. Sauber lösen =
  Feld an der Agenten-Antwort ergänzen (Backend) und Client darauf umstellen.

---

## Rezepte, die heute gebraucht wurden

**Prod-SQL:**
```bash
TOKEN=$(grep -E '^SUPABASE_MCP_TOKEN=' ~/Dev/velgarien-rebuild/.env | cut -d= -f2- | tr -d '"' | xargs)
python3 -c "import json,sys; print(json.dumps({'query': sys.argv[1]}))" "$SQL" \
  | curl -sS -X POST "https://api.supabase.com/v1/projects/bffjoupddfjaljqrwqck/database/query" \
      -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" --data @-
```

**Wegwerf-Postgres mit echtem Schema** (funktionale Migrationsprobe):
`pgvector/pgvector:pg17`, dann von Hand `anon`/`authenticated`/`service_role`/
`supabase_admin`/`authenticator`, Schemata `auth`/`storage`/`extensions`,
`auth.users` MIT `instance_id`, `auth.identities`, `storage.buckets`+`objects`,
`auth.uid()`/`role()`/`jwt()`, `storage.foldername()`, ein Dev-User
`00000000-0000-0000-0000-000000000001`. Dann alle Migrationen der Reihe nach:
281 von 308 laufen durch, die 27 Fehlschläge sind Realtime-Publikationen und
Storage-Details.

**Coolify-Deploy prüfen:**
```bash
ssh -fNL 8000:127.0.0.1:8000 metaspots
curl -s -H "Authorization: Bearer $(tr -d '\n' < ~/.config/metaspots/coolify-api.token)" \
  "http://127.0.0.1:8000/api/v1/deployments/<uuid>"
```

⚠ **Heredoc IMMER quotieren** (`<<'PYEOF'`). Ein unquotierter Heredoc hat
Backticks in SQL-Kommentaren als Befehle ausgeführt und den Inhalt still
gelöscht.

---

## Die zehn offenen Entscheidungen (gebündelt vorzulegen)

1. **Migrationen 295 (Epochen-Klon) + 299 (Chat-Prompt) auf Prod?**
2. **N5-Balance:** Schwellen an die erreichbaren Maxima anpassen ODER
   Moodlet-Stärken vergrößern. Vorher ein Messskript (E11-Vorbild, J7).
3. **9 917 von 20 053 Chronikzeilen** mit englischem Statuswort im deutschen
   Text — Prod-UPDATE mit vier `regexp_replace`?
4. **46 % der Chronik** ist derselbe Narbengewebe-Satz (exakt 1,00 je Tick je
   Welt, bauartbedingt) — Gestaltungsfrage.
5. **11 von 16 lebenden Welten ohne deutschen Titel** — Reparaturskript mit
   Liste, nicht automatisch.
6. **Drei Weltvorlagen `chat_system_prompt`** ohne Erinnerung/Stimmung (W6).
7. **`GET /api/v1/public/bonds`** ohne Frontend-Aufrufer — Ansicht bauen oder
   Endpunkt entfernen?
8. **Kampagnen:** vollständige Backend-Mechanik, 0 Zeilen auf Prod, keine UI.
9. **D13/Bindungen:** `enter_strain` verdrahten (braucht eine Zahl) oder
   löschen?
10. Aus der Vorsitzung offen: B16 Testversand · C2 Journal · `active_agents`
    (30 Agenten gelöschter Welten, N3) · Tagesobergrenze Mails ·
    ladungsfähige Anschrift fürs Impressum · D1 Bureau-Druckformel ·
    D9 Bleed-Auto-Freigabe · G3 sieben März-Epochen archivieren.
    Von der Parallelsitzung: **Persönlichkeits-Rückfüllung** (0,03 USD für
    alle 258 Agenten, zurückgestellt) · **Datenexport** existiert nicht
    (DSGVO-Mail verweist darauf).
